{
    "name": "root",
    "private": true,
    "scripts": {
        "start": "cd server && npm start",
        "heroku-postbuild": "npm --prefix client install && npm --prefix client run build && mkdir -p server/public && cp -r client/dist/e-commerce/browser/* server/public/"
    },
    "engines": {
        "node": "20.x",
        "npm": "10.x"
    }
}